# Add all components here using add_component(<component_directory>)

add_component(MatchCorrespondences)

add_component(ConfidenceCalculator)

ADD_COMPONENT(MatchesVisualization)

ADD_COMPONENT(ImageDarkConv)
